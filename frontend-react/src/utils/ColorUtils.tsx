import { red, teal, lightBlue, lightGreen, amber } from '@mui/material/colors';
import { light } from '@mui/material/styles/createPalette';

const bgcolor_header = teal[500];
const bgcolor_sidemenu = teal[300];
const bgcolor_sidemenu_active = teal[200];

const color_category_project = lightBlue[300];
const color_category_portfolio = lightGreen[300];
const color_category_activity = amber[300];

export {
  bgcolor_header,
  bgcolor_sidemenu,
  bgcolor_sidemenu_active,
  color_category_project,
  color_category_portfolio,
  color_category_activity,
};